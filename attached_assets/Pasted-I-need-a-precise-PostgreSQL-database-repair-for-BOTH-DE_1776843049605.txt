I need a precise PostgreSQL database repair for BOTH DEV and PROD databases.

This must be a SAFE SURGICAL FIX, not a reset.

==================================================
PROBLEM
==================================================

I manually pasted backup rows while trying to fix invoice data, and this corrupted invoice records in both the application and database.

This created:

- wrong client assignments
- duplicated rows
- shifted facture numbering
- incorrect BL links
- incorrect printed facture and BL output
- totals inconsistencies
- broken multi-line invoice grouping
- neighboring invoices overwritten by mistake

==================================================
CRITICAL BUSINESS RULE (VERY IMPORTANT)
==================================================

In every backup row:

COLUMN 1 = BL number (Bon de Livraison)
COLUMN 6 = Facture number

Example:

BL 26/000038 → Facture 26/000037

This is NORMAL and CORRECT.

BL number and Facture number are NOT supposed to be identical.

DO NOT apply validation like:

BL == Facture

This is WRONG.

Correct validation is:

Use the real historical BL ↔ Facture relationship from backup data.

Because:

- some BLs exist only for transportation and may not need a facture
- some factures may exist without a BL
- numbering differences are legitimate and normal

NEVER auto-correct by forcing BL = Facture.

==================================================
SOURCE OF TRUTH (ONLY VALID DATA)
==================================================

Use ONLY the backup rows below as truth.

For these rows:

COLUMN 1 = BL
COLUMN 6 = FACTURE

Correct last sales from backup:

BL 26/000035 → Facture 26/000034
Client: Bachir BALLOUMI
Designation: SPRING ST 124CC BLEU FONCE
Total TTC: 4150.720

BL 26/000036 → Facture 26/000035
Client: Zaid BAILI
Designation: BLASTER GRIS 125cc
Total TTC: 6900.810

BL 26/000037 → Facture 26/000036
Client: Ahmed AAGRBI
Designation: FORZA POWER 124CC BLEU
Total TTC: 2998.800

BL 26/000038 → Facture 26/000037
Client: Taieb KACHOUB
Designation: BLASTER VERT 125cc
Total TTC: 6902.000

BL 26/000039 → Facture 26/000038
Client: Fares BOUKARI
Designation: BLASTER NOIR 125cc
Total TTC: 6900.810

BL 26/000040 → Facture 26/000039
Client: STE SOTUCHOC

This is a MULTI-LINE invoice with exactly 3 lines:

1)
Ref 14
PISTA HR+ VERT MARRON 124 cc
TTC: 4748.100

2)
Ref 15
PISTA HR+ NOIR ROUGE 124 cc
TTC: 4748.100

3)
Ref 20
PISTA HR+ BLEU BEIGE 124 cc
TTC: 4748.100

Combined Total TTC:
14244.300

Continue exactly as:

BL 26/000041 → Facture 26/000040
BL 26/000042 → Facture 26/000041
BL 26/000043 → Facture 26/000042
BL 26/000044 → Facture 26/000043
BL 26/000045 → Facture 26/000044
BL 26/000046 → Facture 26/000045
BL 26/000048 → Facture 26/000046

These mappings are correct and must be preserved.

==================================================
REQUIRED ACTIONS
==================================================

STEP 1 — INSPECT FIRST (NO WRITES)

Inspect BOTH:

- DEV PostgreSQL database
- PROD PostgreSQL database

Check all related tables:

- invoices
- delivery_notes
- invoice_items / invoice_lines
- sales table if separate
- facture linkage tables
- BL linkage tables
- customer relations
- print source queries
- totals generated by joins

NO modifications yet.

Only inspect and report first.

--------------------------------------------------

STEP 2 — CREATE BACKUP SNAPSHOT

Before ANY write operation:

Create a full backup snapshot of:

- all affected invoices
- all affected BL rows
- all affected invoice lines
- related customer assignments
- linkage rows
- print source rows

Need rollback safety before repair.

Absolutely no destructive action before backup.

--------------------------------------------------

STEP 3 — FIND EXACT CORRUPTION START

Detect exactly:

- where overwrite started
- where duplicate insert started
- which rows were shifted
- which rows were overwritten
- whether INSERT happened instead of UPDATE
- whether neighboring invoices were modified
- whether form reuse caused duplicate writes

Do NOT assume.
Verify exactly.

--------------------------------------------------

STEP 4 — COMPARE WITH BACKUP

Use backup values above as authoritative truth.

Compare for each affected record:

- BL number
- Facture number
- client
- designation
- quantity
- unit price
- TVA
- totals
- serial number
- grouped invoice lines
- print output values

--------------------------------------------------

STEP 5 — SAFE SURGICAL REPAIR ONLY

Fix ONLY corrupted rows.

Restore exactly:

- BL number
- Facture number
- client
- invoice totals
- invoice lines
- serial numbers
- customer assignment
- BL ↔ Facture mapping

Preserve:

- unrelated valid invoices
- legitimate BL without facture
- legitimate facture without BL

DO NOT reset the database.

--------------------------------------------------

STEP 6 — REMOVE BAD DUPLICATES ONLY

Delete ONLY:

- accidental duplicate rows from bad paste
- duplicate invoice lines
- duplicate BL links
- orphan rows created by bad import
- overwritten shadow rows if duplicated

Do NOT remove valid business records.

--------------------------------------------------

STEP 7 — CRITICAL MULTI-LINE FACTURE FIX

Very important:

BL 26/000040 → Facture 26/000039
Client: STE SOTUCHOC

must remain exactly:

- 3 invoice lines
- properly grouped
- correct total = 14244.300
- correct printed facture output
- correct printed BL output

No split records.
No duplicate rows.
No partial totals.

--------------------------------------------------

STEP 8 — VERIFY NEXT NUMBERING

Do NOT renumber history.

Only ensure future creation continues safely from:

Latest valid BL:
26/000048

Latest valid Facture:
26/000046

without collision.

No forced historical renumbering.

--------------------------------------------------

STEP 9 — FIX PRINT OUTPUT

Printed:

- facture
- bon de livraison

must display exactly restored DB values.

Current UI print mismatch must be corrected.

Check:

- joins
- grouping
- duplicated line rendering
- wrong customer joins
- stale cached frontend values

Fix source query if needed.

--------------------------------------------------

STEP 10 — ROOT CAUSE ANALYSIS

Audit exactly why corruption happened.

Check:

- manual edit flow
- insert vs update bug
- form state reuse
- duplicate prevention failure
- BL relation overwrite issue
- stale frontend state
- backend mutation logic
- save handler logic
- print query mismatch

Explain exact root cause.

--------------------------------------------------

STEP 11 — ADD PROTECTION

Implement prevention so this never happens again.

A manually edited facture must NEVER:

- overwrite neighboring invoices
- duplicate invoice lines
- duplicate BL assignments
- shift numbering accidentally
- break BL ↔ Facture mapping
- corrupt print output

Add:

- safe update validation
- duplicate prevention
- transaction rollback safety
- protected BL linkage validation
- update-only mode for manual edits

==================================================
IMPORTANT RESTRICTIONS
==================================================

DO NOT:

- reset the whole database
- modify unrelated invoices
- force BL = Facture
- renumber everything
- guess missing values
- perform destructive actions before backup

ONLY:
repair corrupted records precisely.

==================================================
FINAL REQUIRED OUTPUT
==================================================

Provide a final repair report including:

1. Exact corrupted rows found
2. Before vs After values
3. Duplicates removed
4. Wrong links repaired
5. BL ↔ Facture mapping restored
6. Multi-line facture verification
7. Print output verification
8. Root cause explanation
9. Protection added to prevent recurrence

Use backup rows as the ONLY source of truth.

NO assumptions.
NO global reset.
SAFE SURGICAL FIX ONLY.